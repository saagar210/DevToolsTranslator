{
  "name": "@dtt/schemas",
  "private": true,
  "version": "0.1.0",
  "description": "Shared schema constants",
  "type": "module",
  "scripts": {
    "lint": "pnpm run typecheck",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run typecheck",
    "build": "tsc -p tsconfig.build.json"
  }
}
